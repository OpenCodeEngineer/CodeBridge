apiVersion: apps/v1
kind: Deployment
metadata:
  name: codex-bridge-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codex-bridge-worker
  template:
    metadata:
      labels:
        app: codex-bridge-worker
    spec:
      containers:
        - name: worker
          image: codex-bridge:latest
          env:
            - name: ROLE
              value: worker
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: codex-bridge-secrets
                  key: database_url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: codex-bridge-secrets
                  key: redis_url
            - name: CONFIG_PATH
              value: /app/config/tenants.yaml
            - name: CODEX_API_KEY
              valueFrom:
                secretKeyRef:
                  name: codex-bridge-secrets
                  key: codex_api_key
            - name: GITHUB_APP_ID
              valueFrom:
                secretKeyRef:
                  name: codex-bridge-secrets
                  key: github_app_id
            - name: GITHUB_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: codex-bridge-secrets
                  key: github_private_key
